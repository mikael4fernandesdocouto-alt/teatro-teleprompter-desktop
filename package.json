{
  "name": "teatro-teleprompter",
  "version": "3.0.0",
  "description": "Teatro Teleprompter - App Desktop com Vosk offline",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . Teatro-Teleprompter --platform=win32 --arch=x64 --out=dist --overwrite --asar=false --prune=false"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "vosk": "^0.3.39"
  }
}
